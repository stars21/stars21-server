server:
  port: 8080
  session-timeout: 30
  tomcat.max-threads: 0
  tomcat.uri-encoding: UTF-8

spring:
  datasource:
    url : jdbc:mysql://127.0.0.1:3306/test?useUnicode=true&characterEncoding=utf-8&useSSL=false&zeroDateTimeBehavior=convertToNull
    username : root
    password : 123456
    driverClassName : com.mysql.jdbc.Driver

token:
  header: Authorization
  secret: secret
  expiration: 604800

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: cn.stars21.model